import { PageShell } from "@/components/shared/page-shell";

export default function AboutPage() {
  return (
    <PageShell
      title="About Us"
      description="Content will load from the StaticPage model (slug: about)."
    />
  );
}
