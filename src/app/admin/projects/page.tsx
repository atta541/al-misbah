import { PageShell } from "@/components/shared/page-shell";

export default function AdminProjectsPage() {
  return (
    <PageShell
      title="Projects"
      description="Manage projects and project images (Project, ProjectImage)."
    />
  );
}
