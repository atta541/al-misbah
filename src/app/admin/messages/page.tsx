import { PageShell } from "@/components/shared/page-shell";

export default function AdminMessagesPage() {
  return (
    <PageShell
      title="Contact Messages"
      description="View and manage inquiries from the ContactMessage model."
    />
  );
}
