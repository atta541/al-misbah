import { PageShell } from "@/components/shared/page-shell";

export default function AdminHeroPage() {
  return (
    <PageShell
      title="Hero Slides"
      description="Manage homepage carousel slides (HeroSlide model)."
    />
  );
}
